export const BASE_URL = process.env.NEXT_PUBLIC_BASE_URL
export const VIDEO_LIMIT = 10
export enum Routes {
  HOME = '/',
  PROFILE = '/profile',
  DETAIL = '/detail',
  UPLOAD = '/upload',
  SEARCH = '/search',
}
